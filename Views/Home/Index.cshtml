@using System.Text.Json
@model List<KhalawanyTube.Models.MediaClip>
@{
    var data = Model.Select(clip => new
    {
        id = clip.Id,
        title = clip.Title,
        description = clip.Description,
        mediaType = clip.MediaType,
        filePath = clip.FilePath,
        ownerDisplayName = clip.Owner?.DisplayName,
        createdAtLocal = clip.CreatedAtUtc.ToLocalTime().ToString("yyyy-MM-dd HH:mm"),
        isShared = clip.IsShared
    });
}

<div id="react-home"></div>
<script id="home-feed-data" type="application/json">@Html.Raw(JsonSerializer.Serialize(data))</script>
